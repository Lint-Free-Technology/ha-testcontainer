# Themes directory
#
# Place Home Assistant theme YAML files here.  They are automatically
# loaded via the !include_dir_merge_named directive in configuration.yaml.
#
# Each file should contain a mapping of theme names to theme variables.
# Example:
#
#   My Custom Theme:
#     primary-color: "#03a9f4"
